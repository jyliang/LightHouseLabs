// RainView.js

import { requireNativeComponent } from 'react-native';

// requireNativeComponent automatically resolves 'RNTRain' to 'RNTRainManager'
module.exports = requireNativeComponent('RNTRain', null);